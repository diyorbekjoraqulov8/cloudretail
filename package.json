{
  "name": "cloudretail",
  "workspaces": [
    "apps/api",
    "apps/ui"
  ],
  "scripts": {
    "format": "prettier --ignore-unknown --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "prettier": "^3.4.2"
  }
}
